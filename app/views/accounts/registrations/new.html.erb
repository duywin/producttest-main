<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sign Up</title>
  <style>
      /* Set the background image for the whole section */
      .signup-section {
          background-image: url('https://images.pexels.com/photos/40784/drops-of-water-water-nature-liquid-40784.jpeg');
          background-size: cover;
          background-position: center;
          height: 100vh; /* Full viewport height */
          display: flex;
          align-items: center;
          justify-content: center;
      }

      /* Center the form container */
      .signup-form-container {
          background-color: rgba(255, 255, 255, 0.9); /* Light skin background with slight transparency */
          padding: 2rem;
          border-radius: 8px;
          box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
          width: 100%;
          max-width: 400px; /* Set a max width for the form */
          text-align: center;
      }

      /* Style form groups */
      .form-group {
          margin-bottom: 1rem;
      }

      /* Style form labels and inputs */
      .form-group label {
          display: block;
          margin-bottom: 0.5rem;
      }

      .form-group input {
          width: 100%;
          padding: 0.75rem;
          border: 1px solid #ccc;
          border-radius: 4px;
      }

      /* Style submit button */
      .btn-signup {
          background-color: #007bff;
          color: white;
          border: none;
          padding: 0.75rem 1.5rem;
          border-radius: 4px;
          cursor: pointer;
      }

      .btn-signup:hover {
          background-color: #0056b3;
      }

      /* Style signup link */
      p a {
          color: #007bff;
      }

      p a:hover {
          text-decoration: underline;
      }

      /* Style error messages */
      .error {
          color: red;
          font-size: 0.875rem;
      }
  </style>
</head>
<body>
<section class="signup-section">
  <div class="signup-form-container">
    <h2>Sign Up</h2>
    <p>Please enter your details to create an account!</p>

    <%= form_with model: @account, url: account_registration_path, method: :post, local: true, html: { id: 'signup-form' } do |f| %>
      <div class="form-group">
        <%= f.label :username %>
        <%= f.text_field :username, placeholder: 'Username' %>
        <% if @account.errors[:username].present? %>
          <div class="error"><%= @account.errors[:username].first %></div>
        <% end %>
      </div>

      <div class="form-group">
        <%= f.label :email %>
        <%= f.email_field :email, placeholder: 'Email' %>
        <% if @account.errors[:email].present? %>
          <div class="error"><%= @account.errors[:email].first %></div>
        <% end %>
      </div>

      <div class="form-group">
        <%= f.label :password %>
        <%= f.password_field :password, placeholder: 'Password' %>
        <% if @account.errors[:password].present? %>
          <div class="error"><%= @account.errors[:password].first %></div>
        <% end %>
      </div>

      <div class="form-group">
        <%= f.label :password_confirmation %>
        <%= f.password_field :password_confirmation, placeholder: 'Confirm Password' %>
        <% if @account.errors[:password_confirmation].present? %>
          <div class="error"><%= @account.errors[:password_confirmation].first %></div>
        <% end %>
      </div>

      <div>
        <%= f.submit 'Sign up', class: 'btn-signup' %>
      </div>

      <p>Already have an account? <a href="<%= new_account_session_path %>">Log In</a></p>
    <% end %>

  </div>
</section>
</body>
</html>
